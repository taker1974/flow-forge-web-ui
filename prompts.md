# Prompts

## Stage 0: 2026-03-14

ты - веб-разработчик frontend.

этап 0: тест основной функции.

создай страницу с функциональностью, минимально аналогичной n8n или make:

- верх: топ с 10-ю кнопками типа "lorem ipsum", без функций или с вызовом alert (тулбар);

- слева: библиотека компонентов в виде прямоугольников с произвольными названиями; штук 10 разного цвета;
- справа, основное поле: канвас, куда можно перетаскивать (копировать из) компоненты из библиотеки; добавленные компоненты-прямоугольники можно свободно перетаскивать по канвасу; пусть размер канваса будет ограничен каким-то разумным пределом.

реализация - на чём угодно.
предполагается, что будет динамика, будет изменение содержимого компонентов в канвасе. но это потом.

## Stage 1: 2026-03-16, resizing

ты - веб-разработчик frontend.

следующий этап: внедрение функции изменения размеров компонентов на канвасе.

сейчас в основное поле можно добавлять компоненты-прямоугольники, перетаскивать их. это хорошо.
добавь возможность изменять изменять размеры компонентов-прямоугольников на канвасе: пусть границы прямоугольников будут активными.
пусть размеры прямоугольников будут ограничены так:

- наименьшие ширина и высота: 70 и 30;
- наибольшие ширина и высота: 250 и 200.

прямоугольники не должны выходить за пределы канваса (сейчас не выходят, и не должны выходить после внедрения функции изменения размеров).

---

доработай компоненты:

- уменьши раза в 2 радиусы скруглений для нижнего и верхнего левого угла и для верхнего правого угла;
- пусть радиус скругления для правого нижнего угла станет 1 или 2;
- измени форму хэндла изменения размера: русть это будет прямоуголный треугольник справа внизу.

---

- введи константу кратности размеров канваса равную 10;
- сделай размеры канваса кратными этой константе;

- введи константу кратности размеров компонента, равную 5;
- сделай размеры компонентов кратными этой константе;
- сделай так, чтобы при изменении размеров компонентов ширина и высота компонента оставались кратными этой константе;

- введи константу кратности положения компонента, равную 5;
- сделай так, чтобы при изменении положения компонента на канвасе его координаты были кратными этой константе.

## Stage 2: 2026-03-16, lines

ты - веб-разработчик frontend.

следующий этап: внедрение функции соединения компонентов линиями.

добавь функцию соединения компонентов линиями.
для этого можно использовать активные области (небольшие окружности или прямоугольники) в середине каждой из сторон компонента.

- при нажатии ЛКМ и удержании по активной области начинаем рисовать линию;
- при отпускании ЛКМ на активной области завершаем рисовать линию;
- при нажатии ПКМ на активной области удаляем все линии, связанные с этой областью.

при этом каждая линия имеет начало и конец:

- начало ассоциативно связано с компонентом, на котором мы начали создавать линию;
- конец ассоциативно связан с компонентом, где мы завершили рисовать линию.

Error:

ошибка:
"ЛКМ по коннектору и удержание начинает рисовать линию; ведёшь мышь к другому коннектору..." - к сожалению, после ЛКМ, удержания и перемещения начинает перемещаться компонент.
попробуй исправить это.

## Stage 3: 2026-03-17, attributes

ты - веб-разработчик frontend.

следующий этап: добавлениие атрибутов к компонентам (блокам).

каждый компонент (теперь он будет называться "блок") должен иметь следующие атрибуты (поля):

- internal_block_id; целое число, значение глобального счётчика блоков на канвасе в виде строки; присваивается каждому блоку при перетаскивании его на канвас; уникальное значение минимальной длиной 1 и максимальной 16 символов;
- block_type_id; строка длиной от 4 до 64 символов; пусть пока это будет название компонента в библиотеке из левой части окна;
- default_input_text; произвольная строка длиной от 4 до 4096 символов; пусть пока будет из разряда "lorem ipsum";
- input_text; произвольная строка длиной от 4 до 4096 символов; пусть пока будет пустой.

надписи на блоке в канвасе теперь должны выглядеть примерно так:

|-----------------------------------------|
| block_type_id (internal_block_id)       |
|-----------------------------------------|
| input_text [это должно быть текстовое   |
| поле ввода]                             |
|-----------------------------------------|

---

нужна небольшая доработка.
сейчас заголовок блока выглядит так:

| block_type_id (internal_block_id)     #какой-то_индекс_в_скруглённом_блоке |

нужно оставить только один ИД блока. пусть internal_block_id останется там, где сейчас находится како-то индекс после символа "#".

## Stage 4: 2026-03-17, UI for full template

ты - веб-разработчик frontend.

следующий этап: доработка UI для создания полного шаблона проекта.

давай доработаем верхнюю часть окна с кнопками.

добавь enum TemplateStatus с такими значениями:
    /** Template status is unknown: not set, wrong value, etc. */
    UNKNOWN("UNKNOWN"),
    /** Template is ready for use. */
    READY("READY"),
    /** Template is being edited. */
    EDITING("EDITING"),
    /** Template has been deleted. */
    DELETED("DELETED"),
    /** Template has been rejected. */
    REJECTED("REJECTED");

пусть будут такие кнопки:

- "New template"
- "Save this template"
- "Show my templates"

во втором ряду, под кнопками, будут поля:

- выпадающий выбор "Статус шаблона" со значениями из TemplateStatus;
- однострочное поле ввода "Название шаблона" длиной от 4 до 64 символов.

---

добавь колонку справа, напротив библиотеки компонентов. колонка должна выглядеть так:

--- Мои шаблоны ---------
здесь будет список моих  
шаблонов (позже)
...
...

-------------------------
кнопки "Load", "Delete", "Run..."
-------------------------
миникарта 
(сейчас она просто висит 
в правом нижнем углу)
-------------------------

измени верхнюю часть:

- убери кнопку "Show my templates";
- объедини ряд кнопок со вторым рядом, где TemplateStatus и название шаблона.

---

добавь в нижнюю часть скрываемый раздел с текстовым полем. типа toggle panel в Cursor.
пусть этот раздел будт под канвасом, между левой и правой колонками.
переключатель видимости размести справа от верхнего ряда управляющих элементов.

пусть раздел (панель) называется "Journal".
внутри панели - текстовое поле только для чтения. пусть пока выводится "lorem ipsum".

---

убери из данных блока input_text.
там, где был input_text, должен быть default_input_text (включая поле ввода в блоке).

---

давай добавим в данные линии:

- "internal_line_id": строка вида "<id блока начала>-<id блока конца>"
- "block_from_internal_id": <id блока начала>
- "block_to_internal_id": <id блока конца>

---

реализуем реакцию на кнопку "Save this template".
при нажатии проверим поля "Статус шаблона" и "Название шаблона":

- статус не должен быть UNKNOWN;
- название должно иметь длину от 4 до 64 символов.

если не так, то выведем красивый алерт с указанием на ошибку.

канвас может быть пустым.

если всё в порядке, то сформируем такой json:

{
  "name": <Название шаблона>,
  "data": {
    "status": <Статус шаблона>,
    "blocks": [
      // далее для каждого блока из канваса (из данных):
      {
        "internal_block_id": <internal_block_id>,
        <block_type_id>,
        "default_input_text": <default_input_text>
      },
      ...
    ],
    "lines":[
      // далее для каждой линии из канваса (из данных):
      {
        "internal_line_id": <internal_line_id>,
        "block_from_internal_id": <block_from_internal_id>,
        "block_to_internal_id": <block_from_internal_id>
      }
    ]
  }
}

затем текст с этим json надо вывести в текстовое поле в Journal, предварительно форсировав открытие этого элемента.

---

теперь придумай аналогичный json для того, чтобы сохранять позиции и размеры блоков, а также линии, соединяющие блоки.
для блоков в этом json не надо сохранять default_input_text.

впоследствии из этого json будет восстанавливаться состояние канваса со всеми элементами, состояние данных вообще.

придуманный json выводи в то же место, через запятую и через пустую строку после первого json.

## Stage 5: 2026-03-18, register and login

ты - отличный веб-разработчик frontend!

следующий этап: внедрение регистрации и входа.

есть бэкэнд user-service с настройками в docker compose вот здесь:
./flow-forge/docker-compose.yml
наверное, надо создать какой-то конфиг с url этого сервиса для использования в коде (на твоё усмотрение).

проект user-service находится здесь:
./flow-forge/flow-forge-user/

проект со всеми dto находится здесь:
./flow-forge/flow-forge-dto/

в RegisterRequestDto ты увидишь passwordHash: пользователь вводит какой-то понятный для себя пароль, а затем код на основе этого введённого пароля формирует какой-нибудь хороший хэш для передачи на бэкэнд.
с остальными полями всё должно быть понятно.
с dto для входа с тем же полем всё так же.

обрати внимание, что мы будем работать с токенами.

надо:

- добавить кнопки "Register", "Login" и "Logout" справа вверху, там, где кнопка "Journal"; логика видимости кнопок - самая очевидная, без особенностей кроме того, что login выполняется отдельным (следующим) действием после регистрации (то есть автоматический логин после регистрации не выполнять);
- по кнопке "Register" вызывать окно регистрации со всеми полями; после отправки данных сообщать красивым алертом о результате;
- по кнопкам "Login" и "Logout" выполнять соответствующие действия: запрашивать логин и пароль, молча вызывать logout.

---

да, сделай ещё:

- добавь бэйдж со статусом авторизации;
- в user-service есть метод refresh: вызывай его каждые 10 (настраивается в конфиге?) минут.

и ещё:

- если в поле ввода пароля, в значении, указан префикс "hash:", то для хэша используй введённую строку после префикса; это и в регистрации, и во входе; мне это нужно для отладки.

---

добавь в поле ввода пароля кнопку для максировки/демаскировки вводимых символов.

---

я хочу запускать страницу во вкладке IDE, в Cursor, чтобы ты исправлял всё в онлайне.
напомни в двух словах, как это делать.

---

добавь к канвасу автоматическую горизонтальную прокрутку.

## Stage 6: 2026-03-19, logo

давай придумаем, как может выглядеть мой логотип и логотип этого приложения.

по логотипу компании.
моя компания называется TKSoft - от Терских Константин. есть варианты другие, без фамилии: AKSoft, AK-Soft - от Арсений и Константин). 
сайт - tksoft.spb.ru. я из города Санкт-Петербург, Россия.

по логотипу приложения.
визуально это аналог n8n и т.п. инструментов low/no code. пользователь видит блоки и линии (алгоритмы в визуальном представлении).

я хочу идеи в виде изображений в logo-ideas.html. туда можно css, svg etc.
можешь придумать штук по 10 для лого компании и для лого приложения?

можно делать долго, можно искать аналоги в интернете.

## Stage 7: 2026-03-20, local persistence

@index.html
@chat.md
@prompts.md

ты - отличный веб-разработчик frontend!

давай реализуем локальное сохранение состояния канваса.

сейчас я могу перетягивать блоки из библиотеки на кавнас, могу соединять блоки линиями, могу менять размеры блоков.
но если я нажму кнопку "Обновить эту страницу" в браузере, то канвас будет очищен.

надо иметь возможность очищать канвас принудительно (то есть очищать данные приложения), но надо делать это контролируемо.

какие решения ты можешь предложить по сохранению состояния канваса (блоки с их координатами и размерами, линии связей между блоками, размеры самого канваса)?

я вижу так:

- добавляем блок на канвас - он сохраняется и в массив в коде, и в локальное хранилище;
- с линиями то же самое, что и с блоками;
- с самим канвасом, с его размерами (сейчас изменять сам канвас можно только в смысле его размеров) - тоже сохраняем и в переменные, и в локальное хранилище;
- с удалением то же самое - удаляем и в массивах в коде, и в хранилище;
- при создании нового шаблона (проекта; по кнопке "New template" или при загрузке проекта из бэкэнда) предварительно очищаем всё в случае успеха загрузки.

что думаешь? какой план?

## Stage 0: 2026-03-14

ты - веб-разработчик frontend.

этап 0: тест основной функции.

создай страницу с функциональностью, минимально аналогичной n8n или make:

- верх: топ с 10-ю кнопками типа "lorem ipsum", без функций или с вызовом alert (тулбар);

- слева: библиотека компонентов в виде прямоугольников с произвольными названиями; штук 10 разного цвета;
- справа, основное поле: канвас, куда можно перетаскивать (копировать из) компоненты из библиотеки; добавленные компоненты-прямоугольники можно свободно перетаскивать по канвасу; пусть размер канваса будет ограничен каким-то разумным пределом.

реализация - на чём угодно.
предполагается, что будет динамика, будет изменение содержимого компонентов в канвасе. но это потом.

## Stage 7: 2026-03-23, variouse block types

@prompts.md
@chat.md
@index.html

ты - веб-разработчик frontend.

очередной этап: конкретизация типов блоков.

сейчас на канвас могут быть добавлены блоки типа "lorem ipsum" из библиотеки слева.
пришла пора конкретизации. теперь в библиотеке слева должны быть только такие блоки:

- block_type_id = "Constants"; основной цвет - серый или цвет фона окна; максимальные размеры (ширина и высота): 700 x 500;
- block_type_id = "Variables"; основной цвет - серый или цвет фона окна, но намного светлее; максимальные размеры (ширина и высота): 700 x 500;
- block_type_id = "Watch"; основной цвет - почти чёрный; максимальные размеры (ширина и высота): 500 x 300;
- block_type_id = "Start"; основной цвет - оранжевый;
- block_type_id = "Finish"; основной цвет - синий;
- block_type_id = "Expressions"; основной цвет - голубой;
- block_type_id = "Conditions"; основной цвет - голубой, но светлее, чем expressions;
- block_type_id = "Delay"; основной цвет - голубой, тёмно-голубой;
- block_type_id = "Confirmation"; основной цвет - жёлтый, светлый;
- block_type_id = "AI"; основной цвет - ярко-белый, текст ввода - тёмно-синий; максимальные размеры (ширина и высота): 1200 x 700.

---

есть ошибки работы с размерами, с ограничениями по размерам. 
на примере блока констант:

- при первом размещении блока его размеры можно менять, но только в старых ограничениях; ограничения в 700 на 500 px не реализованы;
- после размещения блока, после смены фокуса или чего-то ещё, размер блока практически невозможно изменить.

надо доработать.

---

сделай, пожалуйста, ещё одно псевдоокно справа, под "окном" "MINI-MAP, в той же колонке.
выведи окно "Properties" с таким содержимым для выбранного блока:

- internal_block_id (read-only, ro);
- block_type_id (ro);
- default_input_text (ro);
- размеры блока в виде ширина x высота px (ro).

---

ок, и давай сделаем это чуть компактнее плюс добавим положение на канвасе. 
по большей части надо оперенести заголовки и содержимое в одну строку типа "название или заголовок | значение":

- "ID" | <internal_block_id>
- "Type" | <block_type_id>
- "Default input text" [отдельная строка заголовка]
- <default_input_text>
- "Position" | <здесь надо указать ещё и положение на канвасе в виде X,Y px>
- "Size" | <width x height> px

---

ширина заголовков слишком большая - надо сделать меньше процентов на 30.
для заголовка "Default input text" можно оставить, как есть.

---

давай для каждого типа блока установим минимум ширины и высоты равным 200 на 130 px.
и лучше сделать это именно для каждого типа блока отдельно, а не единой переменной: есть вероятность, что позже мы будем подгонять эти параметры для каждого типа блока.

---

по блокам Start и Финиш:

- убери у этих блоков поля ввода для default_input_text;
- вместо поля ввода вставь символ 🏁 с выравниванием по центру;
- установи фиксированные размеры этих блоков в 200 x 100 px;
- убери у них хэндл для ресайза из правого нижнего угла.

---

по блокам Start и Финиш установи фиксированные размеры этих блоков в 200 x 80 px

---

для блоков Conditions и Expressions минимумы и максимумы размеров скопируй из блока Constants

для блоков Delay и Confirmation минимумы и максимумы размеров скопируй из блока Watch

по блокам Start и Финиш: давай установим все 4 радиуса по углам блока в 15 px

блоки Constants, Variables, Watch: давай скроем у них метки прикрепления линий, чтобы от них и к ним нельзя было тянуть линии

---

ещё точечно доработаем UI.

Template status:

- давай сделаем дефолт (по New template или первый раз, если в локальной хранилище ничего нет) равным EDITING;
- для значения UNKNOWN фон сделаем magenta;
- для значения REJECTED фон сделаем красным, а шрифт белым;
- для значения EDITING фон сделаем голубым;
- для значения READY фон сделаем салатовым.
- для значения DELETED фон сделаем чёрным, а шрифт серым.

Template name:

- значение длины имени шаблона - от 4 до 64 символов; если длина имени не такая, то   сделаем фон красным.

---

давай оптимизируем используемое пространство.

в библиотеке:

- сделаем блоки Уже (по горизонтали) на 30%;
- отступы по горизонтали сделаем меньше (вполовину) и для самих блоков библиотеки, и для внутренних отступов панели библиотеки (только пор гризонтали);

в правой колонке, там, где "My templates", "Mini-map" и "Properties":

- сделаем отступы по горизонтали Уже в два раза;
- расширим всю эту колонку по горизонтали в 1.5 раза.

убери фразу "10 prebuild" из заголовка библиотеки
уменьши ширину колонки библиотеки на 30%

по панели "My templates".
все элементы панели должны быть активны (enabled) только тогда, когда пользователь залогинен. (я не знаю, можно ли дизэйблить всю панель разом.)
если вход не выполнен, то всё задизэйблено.
точно так, как на кнопке "Logged out" (Logout).

## Stage 8: 2026-03-23, template list

@prompts.md
@chat.md
@index.html

ты - веб-разработчик frontend.

очередной этап: работа с API списка шаблонов.

есть бэкэнд template-service с настройками в docker compose вот здесь:
./flow-forge/docker-compose.yml
наверное, надо создать какой-то конфиг с url этого сервиса для использования в коде (на твоё усмотрение).

исходники проекта template-service находятся здесь:
./flow-forge/flow-forge-backend-template/

проект со всеми dto находится здесь:
./flow-forge/flow-forge-dto/

мы будем использовать метод /list контроллера TemplateController.
вспомни, что для работы с API мы используем токены.

как ты можешь видеть, метод возвращает TemplateListResponseDto с массивом TemplateListItemResponseDto внутри.

в TemplateListItemResponseDto нас сейчас интересуют поля name и status.

я вижу работу так:

- при открытии/обновлении страницы, если есть токен (если выполнен вход), то пытаемся вызвать /list;
- то же самое пытаемся делать сразу после входа (после выполнения логина);
- то же самое пытаемся делать при нажатии кнопки Update в "Mt templates";

- если метод выполнен успешно, то заполняем список шаблонов элементами вида "<name> (<status>)";
- если метод выполнен с ошибкой в случае, если токен точно есть, то отображаем красивый алерт с выводом причины ошибки;

- если выполнен выход (logout), то очищаем список;

- пытаемся обновить список каждый раз при сохранении шаблона по "Save this template".

## Stage 9: 2026-03-23, save this template

@prompts.md
@chat.md
@index.html

ты - веб-разработчик frontend.

очередной этап: реализация функции "save this template"

1. предварительно надо сделать то же самое, что и с панелью "My templates": привязать enabled кнопки "save this template" к факту входа (логину) на сервер (можно просто взять всю логику из "My templates").

2. есть бэкэнд template-service с настройками в docker compose вот здесь:
./flow-forge/docker-compose.yml
наверное, надо создать какой-то конфиг с url этого сервиса для использования в коде (на твоё усмотрение).

исходники проекта template-service находятся здесь:
./flow-forge/flow-forge-backend-template/

проект со всеми dto находится здесь:
./flow-forge/flow-forge-dto/

мы будем использовать эти методы контроллера TemplateController:

- /create для создания ещё не сохранённого шаблона и
- /update для сохранения уже сохранённого (созданного ранее) шаблона

и затем метод /set-or-update контроллера TemplateVisualsController для сохранения визуального представления шаблона.

вспомни, что для работы с API мы используем токены.
в случае ошибки отображаем красивый алерт с выводом причины ошибки.

оба метода возвращают одинаковые [пока] DTO, в которых нас интересует поле templateId.

я вижу работу так:

- в данных UI есть Long templateId, который может быть пустым или со значением;

- при нажатии "save this template", кроме работы с панелью "Journal" (пока оставим эту панель и её логику), после проверок Template state и name, вызываем один из методов: если уже есть templateId, то вызываем /update, иначе - /create;
- если create успешен, то запоминаем новый templateId;
- если любой метод выполнен с ошибкой, то отображаем красивый алерт с выводом причины ошибки;

- пытаемся обновить список каждый раз при сохранении шаблона по "Save this template".

сразу после успешного сохранения/обновления шаблона вызываем второй метод - /set-or-update.
в dto для этого метода мы передаём имя шаблона name и строковое представление json (его ты уже генерируешь по "save this template" вслед за генерацией json для логики блоков и линий).
в случае неуспеха делаем то же самое, что и для первого эиапа сохранения-обновления - выводим красивый алерт с расшифровкой ошибки.

---

1. пусть имена типов блоков начинаются с прописных символов: например "constants" вместо "Constants".

2. давай заведём таблицу соответствия block_type_id отображаемому имени (строке) в виде block_type_id -> отображаемое имя:

"constants" -> "Constants"
"variables" -> "Variables"
"watch" -> "Watch"
"start" -> "Start"
"finish" -> "Finish"
"expressions" -> "Expressions"
"conditions" -> "Conditions"
"delay" -> "Delay"
"confirmation" -> "Confirmation"
"assistant-ai" -> "AI"

сейчас почти все имена совпадают, но в будущем это может измениться.

аналогичную таблицу можно завести (если это уже не сделано) для категорий блоков:

"constants" -> "System"
"variables" -> "System"
"watch" -> "Debug"
...
etc.

таким образом, отображаемые в блоках заголовки - это значения из таблиц по именам типов блоков.

3. в json, в поля block_type_id, будем выводить имена типов (ключей?) из таблицы "block_type_id -> отображаемое имя".

4. для блоков start и finish задай, пожалуйста, default_input_text равным именам их типов.

---

выяснил, что основа ошибки при создании - повторяющиеся ключи.
в json шаблона, в разделе "lines", сейчас могут встречаться линии с одинаковыми internal_line_id.
и в UI можно направить сколько угодно линий от одного блока к другому.
при этом у всех линий будут одинаковые internal_line_id типа "7-10".

давай при создании каждой линии проверять уникальность будущего internal_line_id.
если такой internal_line_id уже есть, то просто молча не создаём такую линию.

---

выведи справа от поля ввода "template name"
Saved with ID <currentTemplateId>, если id есть

---

сейчас в верхней панели есть TemplateStatus и name для шаблона:

TEMPLATE STATUS     | TEMPLATE NAME (бэйдж с ID)
<выпадающий список> | [поле ввода имени]

давай сначала отображать колонку с именем, а за ней уже колонку со статусом.

## Stage 10: 2026-03-24, load template

@prompts.md
@chat.md
@index.html

ты - веб-разработчик frontend.

очередной этап: реализация функции загрузки шаблона

есть бэкэнд template-service с настройками в docker compose вот здесь:
./flow-forge/docker-compose.yml
наверное, надо создать какой-то конфиг с url этого сервиса для использования в коде (на твоё усмотрение).

исходники проекта template-service находятся здесь:
./flow-forge/flow-forge-backend-template/

проект со всеми dto находится здесь:
./flow-forge/flow-forge-dto/

для работы с API мы используем токены.
всегда в случае ошибки запроса отображаем красивый алерт с выводом причины ошибки.

мы будем использовать методы

/get контроллера TemplateController для загрузки "логики" блоков и линий, и метод  
/get контроллера TemplateVisualsController для загрузки визуальной части (позиции блоков, размеры и т.п.)

/get контроллера TemplateVisualsController вернёт name и data.
data содержит строку с json визуального представления канваса, блоков, линий и т.п.

я вижу работу так:

- есть список шаблонов с элементами типа TemplateListItemResponseDto, в которых нас интересует поле name;
- по нажатию кнопки load внизу списка:
  - проверяем dirty, предлагаем сохранение, обрабатываем этот кейс;
  - получаем логику и визуальную часть в виде json: если всё удачно, то очищаем текущие данные и восстанавливаем новые данные из json'ов. (здесь потенциально могут быть ошибки, и это с странный случай, но можно предусмотреть какой-нибудь try-catch.)

реализуем?
если не всё понятно, как день, то строй план, задавай уточняющие вопросы.
